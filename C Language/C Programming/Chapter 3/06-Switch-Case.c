#include <stdio.h>

int main()
{
    int rating;

    printf("Enter your rating between (1 to 5)");
    scanf("%d",&rating);

    switch (expression)
    {
    case /* constant-expression */
        /* code */
        break;
    
    default:
        break;
    }
    return 0;
}